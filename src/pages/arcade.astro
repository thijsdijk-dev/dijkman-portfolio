---
import Base from "../layouts/Base.astro";

const pixelDungeonUrl = "/games/pixel-dungeon/";
---

<Base
  title="Arcade: Pixel Dungeon"
  description="Speel Pixel Dungeon via je eigen server op dijkman.dev."
>
  <section class="hero arcade-hero">
    <p class="badge">Arcade</p>
    <h1>Pixel Dungeon</h1>
    <p>
      Deze pagina is nu ingericht voor één game: <strong>Pixel Dungeon</strong>.
      De game wordt geladen vanaf je <strong>eigen serverpad</strong>
      <code>{pixelDungeonUrl}</code>, zodat je niet afhankelijk bent van externe embeds.
    </p>
    <div class="btns">
      <a class="btn" href={pixelDungeonUrl} target="_blank" rel="noreferrer">Start in nieuw tabblad</a>
      <a class="btn" href={pixelDungeonUrl}>Open full-page op deze site</a>
    </div>
  </section>

  <section class="card game-card">
    <h2>Ingesloten gamevenster</h2>
    <p class="muted">
      Als de game nog niet zichtbaar is, controleer of de Pixel Dungeon webbuild op
      <code>/public/games/pixel-dungeon/</code> staat op je server.
    </p>

    <div class="game-wrap">
      <iframe
        title="Pixel Dungeon"
        src={pixelDungeonUrl}
const jsPacmanEmbedUrl = "https://henriquejr.github.io/jspacman/";
---

<Base
  title="Arcade: JSPacman"
  description="Speel JSPacman direct op de arcadepagina van dijkman.dev."
>
  <section class="hero arcade-hero">
    <p class="badge">Arcade</p>
    <h1>JSPacman</h1>
    <p>
      De arcadepagina is volledig vernieuwd en draait nu nog maar één game:
      <strong>JSPacman</strong>. Zelfde donkere stijl, zelfde navigatie, direct speelbaar.
    </p>
    <p class="muted">Tip: klik eerst in het spelvenster om toetsenbordbesturing te activeren.</p>
  </section>

  <section class="card game-card">
    <h2>Speel nu</h2>
    <div class="game-wrap">
      <iframe
        title="JSPacman"
        src={jsPacmanEmbedUrl}
        loading="lazy"
        frameborder="0"
        allowfullscreen
      ></iframe>
    </div>
  </section>

  <section class="card setup-card">
    <h2>Server-checklist</h2>
    <ul>
      <li>Host de Pixel Dungeon webbuild op <code>/games/pixel-dungeon/</code>.</li>
      <li>Zorg dat alle assets relatief laden vanuit die map.</li>
      <li>Test eerst direct op <code>/games/pixel-dungeon/</code>, daarna via <code>/arcade</code>.</li>
    </ul>
  </section>
</Base>

<style>
  .arcade-hero,
  .game-card,
  .setup-card {
  .arcade-hero {
    margin-top: 18px;
  }

  .arcade-hero h1 {
    margin: 10px 0;
  }

  .game-card,
  .setup-card {
  .game-card {
    margin-top: 18px;
    display: grid;
    gap: 12px;
  }

  .game-wrap {
    aspect-ratio: 16 / 10;
    width: 100%;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    overflow: hidden;
    background: #0a0a0f;
  }

  .game-wrap iframe {
    width: 100%;
    height: 100%;
    border: 0;
  }

  ul {
    margin: 0;
    padding-left: 18px;
    display: grid;
    gap: 8px;
  }
</style>
