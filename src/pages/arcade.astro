---
import Base from "../layouts/Base.astro";

// self-hosted endpoints
const retroarchUrl = "/retroarch/";
const uploadUrl = "/retroarch/upload/";

const highlights = [
  {
    title: "Alles-in-één emulator hub",
    text: "Laad verschillende consoles via cores in één uniforme interface, zonder losse tools.",
  },
  {
    title: "Controller ready",
    text: "Gamepads worden direct ondersteund, inclusief remapping en latency-optimalisaties.",
  },
  {
    title: "Savestates & shaders",
    text: "Pauzeer en hervat exact waar je was, en geef klassiekers extra sfeer met filters.",
  },
];

const quickSteps = [
  "Open RetroArch via de knop hieronder.",
  "Kies een core die past bij je game.",
  "Load Content → selecteer je ROM → spelen.",
];

const legalRomSteps = [
  "Gebruik alleen een legale ROM (homebrew, public-domain of je eigen back-up waar je rechten op hebt).",
  "Zet je ROM op je apparaat (pc/laptop/telefoon). Bestanden zoals .nes, .gb, .sfc of .zip.",
  "In RetroArch: Main Menu → Load Core en kies de juiste emulator-core.",
  "Daarna: Main Menu → Load Content → selecteer je ROM → start.",
  "Optioneel: Quick Menu → Save State, en Settings → Input voor controls.",
];
---

<Base
  title="Arcade (RetroArch)"
  description="Start RetroArch direct vanuit de arcade-pagina."
>
  <section class="hero">
    <div class="tag-row">
      <span class="tag">🕹️ Arcade</span>
      <span class="tag">RetroArch</span>
    </div>

    <h1>Arcade</h1>
    <p class="muted lead">
      Start RetroArch direct in je browser. Kies een core, laad content en je bent weg.
      Uploaden kan via de ROM-uploader (alleen met token).
    </p>

    <div class="btns">
      <a class="btn" href={retroarchUrl}>Start RetroArch</a>
      <a class="btn" href={uploadUrl}>Upload ROMs</a>
      <a class="btn" href="/">Terug naar home</a>
    </div>

    <p class="muted footnote">
      Tip: als audio niet meteen werkt: klik één keer in het RetroArch-venster en check tab-audio permissies.
    </p>
  </section>

  <section class="grid">
    {highlights.map((item) => (
      <article class="card">
        <h2>{item.title}</h2>
        <p class="muted">{item.text}</p>
      </article>
    ))}
  </section>

  <section class="card">
    <h2>Snelle start</h2>
    <ol>
      {quickSteps.map((step) => (
        <li>{step}</li>
      ))}
    </ol>

    <details class="details">
      <summary>ROM upload: hoe werkt dat met token?</summary>
      <div class="details-body muted">
        <p>
          De uploadpagina stuurt bestanden naar <code>/retroarch/upload/api/upload</code>.
          Je uploader verwacht een header <code>X-Upload-Token</code>.
        </p>
        <p>
          Voor jezelf: je token staat als <code>UPLOAD_TOKEN</code> in de <code>rom-uploader</code> container.
          Publiceer die token liever niet op je site; behandel ‘m als geheim.
        </p>
      </div>
    </details>
  </section>

  <section class="card">
    <h2>Zo laad je een legale ROM in</h2>
    <p class="muted">
      Gebruik uitsluitend een <strong>legale ROM</strong> waar jij rechten op hebt (homebrew,
      public-domain of een eigen back-up).
    </p>
    <ol>
      {legalRomSteps.map((step) => (
        <li>{step}</li>
      ))}
    </ol>

    <p class="callout">
      Deze pagina moedigt geen illegale downloads aan. Gebruik alleen ROM-bestanden die je legaal mag gebruiken.
    </p>
  </section>
</Base>

<style>
  /* match jouw homepage stijl: simpel, strak, dark cards */
  .hero {
    padding: 28px;
    border: 1px solid #222;
    border-radius: 18px;
    background: #101018;
    color: #f5f7ff;
    margin-top: 18px;
  }

  .tag-row {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 10px;
  }

  .tag {
    display: inline-block;
    padding: 4px 10px;
    border: 1px solid #444;
    border-radius: 999px;
    font-size: 12px;
    opacity: 0.9;
  }

  h1 {
    margin: 0 0 8px;
    font-size: 2rem;
    letter-spacing: -0.02em;
  }

  h2 {
    margin: 0 0 10px;
    font-size: 1.1rem;
    letter-spacing: -0.01em;
  }

  .lead {
    margin: 0;
    max-width: 70ch;
    line-height: 1.65;
  }

  .muted {
    color: #c6ccda;
  }

  .btns {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 14px;
  }

  .btn {
    padding: 10px 12px;
    border: 1px solid #333;
    border-radius: 10px;
    text-decoration: none;
    color: #f5f7ff;
    background: transparent;
  }

  .btn:hover {
    border-color: #777;
  }

  .grid {
    display: grid;
    gap: 14px;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    margin-top: 16px;
  }

  .card {
    padding: 16px;
    border: 1px solid #333;
    border-radius: 14px;
    background: #101018;
    color: #f5f7ff;
    margin-top: 16px;
  }

  .card h2 + p {
    margin: 0;
    line-height: 1.6;
  }

  ol {
    margin: 0;
    padding-left: 20px;
    display: grid;
    gap: 8px;
    line-height: 1.55;
  }

  .footnote {
    margin: 14px 0 0;
    font-size: 14px;
  }

  code {
    background: #111;
    color: #eee;
    padding: 2px 6px;
    border-radius: 6px;
  }

  .callout {
    margin-top: 14px;
    padding: 12px 14px;
    border: 1px solid #2a2a2a;
    border-radius: 12px;
    background: #0f0f16;
    color: #dce2f1;
  }

  .details {
    margin-top: 14px;
    border: 1px solid #2a2a2a;
    border-radius: 12px;
    padding: 10px 12px;
    background: #0f0f16;
    color: #f5f7ff;
  }

  .details summary {
    cursor: pointer;
    user-select: none;
    color: #eef2ff;
  }

  .details-body {
    margin-top: 10px;
    line-height: 1.6;
  }

  .details-body p {
    margin: 0 0 10px;
  }
</style>
