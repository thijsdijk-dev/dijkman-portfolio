---
const { title = "Stappen", items = [] } = Astro.props;
---

<div class="card">
  <h3 style="margin:0 0 10px">{title}</h3>
  <ol class="steps">
    {items.map((it) => <li>{it}</li>)}
  </ol>
</div>

<style>
  .steps{ margin:0; padding-left: 18px; }
  .steps li{ margin: 6px 0; }
</style>
