---
const { items = [] } = Astro.props;
---

<section class="grid">
  {items.map((it) => (
    <div class="card">
      <div class="muted" style="font-size:12px;margin-bottom:6px;">{it.label}</div>
      <div style="font-weight:700">{it.value}</div>
    </div>
  ))}
</section>
